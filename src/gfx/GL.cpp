//
// Created by jay on 7/2/26.
// Based off "Using OpenGL With SDL" by Michael Vance
// https://www.libsdl.org/release/SDL-1.2.15/docs/html/guidevideoopengl.html
//

#include "GL.h"

#include <GL/gl.h>
#include <GL/glu.h>
/*
void GL::setup_opengl(int w, int h) {
    float ratio = (float) w / (float) h;

    //Using Goraud. Switch to Phong later?
    glShadeModel(GL_SMOOTH);

    //Culling
    glCullFace(GL_BACK);
    glFrontFace(GL_CCW);
    glEnable(GL_CULL_FACE);

    glClearColor(0, 0, 0, 0);

    glMatrixModel(GL_PROJECTION);
    glLoadIdentity();
    gluPerspective(60.0, ration, 1.0, 1024)
}

void GL::render() {
}

void GL::draw() {
}
*/